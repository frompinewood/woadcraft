{erl_opts, [debug_info, {parse_transform, lager_transform}]}.
{deps, [
    {gloom, "*", {git, "https://github.com/frompinewood/gloom", {branch, main}}},
    lager
]}.

{shell, [
    % {config, "config/sys.config"},
    {apps, [woadcraft]}
]}.
